package com.RS;

public class Resolution {
    private int width;
    private int height;

    public Resolution(int width, int height) {
        this.width = width;
        this.height = height;
    }

    public int getWidth() {
        return width;
    }

    public int getHeight() {
        return height;
    }
}

/*
 * created the resolution class
 * apply the native resolution of max width and height with
 *  how many pixels wide and how many pixels down to speak
 * Create field of width and height;
 * Create the const with both the fields
 * Create the getters for both the fields
 * Go to the monitor class now.
 */